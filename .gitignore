# Python
__pycache__/
*.py[cod]
.venv/
.pytest_cache/
.mypy_cache/
.ruff_cache/
*.egg-info/
dist/
build/

# uv
# (uv.lock IS committed)

# Node / frontend
node_modules/
frontend/dist/
frontend/dist-*/

# Experiment run data (recorded telemetry) — keep out of git by default
backend/experiments/

# Offline protocol reference downloads (not our code; fetch as needed)
plan/reference/
reference/

# Claude Code session logs / local settings (not for a public repo)
.claude/

# OS
.DS_Store
